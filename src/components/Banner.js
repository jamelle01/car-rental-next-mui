import { Box, Typography } from "@mui/material";

const Banner = () => {
  return (
    <Box>
      <Typography variant="h1">Save big with our cheap car rental!</Typography>
      <Typography variant="h4">
        Top Airports. Local Suppliers. 24/7 Support.
      </Typography>
    </Box>
  );
};

export default Banner;
